# DAB Processing

## Setup

```bash
uv sync
source .venv/bin/activate
```

## Usage

For batch processing
```bash
./run.sh --help
```

For directly processing a single file (it will still output in a directory)
```bash
python dab.py --help
```